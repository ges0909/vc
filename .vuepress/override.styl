// Color configuration
$accentColor = #3eaf7c
$textColor = #2c3e50
$borderColor = #eaecef
$codeBgColor = #282c34

// Layout configuration
$navbarHeight = 3.6rem
$sidebarWidth = 20rem
$contentWidth = 740px

.theme-container.resize-hero-image {
  .home > .hero > img, .logo > img {
    height: 15%;
    width: 15%;
    background-color: powderblue;
      border-radius: 50%;
  }
}
